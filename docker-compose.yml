
services:
  backend:
    build: ./backend
    container_name: ai-backend
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    container_name: ai-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
